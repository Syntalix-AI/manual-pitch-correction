VITE_API_URL=http://localhost:8000

# Copy this file to `.env` for local development and adjust as needed.
# For production builds in CI/EC2, set `VITE_API_URL` to the public API URL
# before running `npm run build` or pass as a Docker build-arg `--build-arg VITE_API_URL`.
